<!DOCTYPE html>
<html lang="en">
    <%- include('layout/header'); -%>
    <%- include('layout/script'); -%>
    <body>
        <!-- Navigation-->
        <% if (!locals.user) { %>
        <%- include('layout/navigation'); -%>
        <% } else { %>
            <% if (user.appointment_id) { %>
                <%- include('layout/driver_navigation_new'); -%>
                <% } else if (locals.userType == "Admin") { %>
        <%- include('layout/admin_navigation'); -%>
        <% } else if (locals.userType == "Driver") { %>
        <%- include('layout/driver_navigation'); -%>
        <% } else if (locals.userType == "Examiner") { %>
        <%- include('layout/examiner_navigation'); -%>
        <% } %>
        <% } %>
        <div class="cover">
            <div>
                <% if (!locals.user) { %>
                <h1>DriveTest Booking Platform</h1>
                <a href="/login"><button class="btn btn-primary">Login / Register</button></a>
                <% } else if(locals.userType =="Driver"){ %>
                    <h2>Welcome to DriveTest Booking Platform</h2>
                    <br>
                    <br>
                <h2><a href="/g"><button class="btn btn-primary">Book Your slot</button></a></h2>
                <% } else if(locals.userType =="Admin"){ %>
                    <h2>Welcome to DriveTest Booking Platform</h2>
                    <br>
                    <br>
                <h2><a href="/appointment"><button class="btn btn-primary">Create Appointment</button></a></h2>
                <% } else if(locals.userType =="Examiner"){ %>
                    <h2>Welcome to DriveTest Booking Platform</h2>
                    <br>
                    <br>
                <h2><a href="/list-appointments"><button class="btn btn-primary">Update Appointment</button></a></h2>
                
                <% } %>
            </div>
        </div>
    </body>
    <!--  footer -->
    <%- include('layout/footer'); -%>
    </html>
